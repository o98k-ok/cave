# cave

A macOS menu bar app for managing SSH tunnels.

## Features

- Manage local and reverse SSH tunnels
- Language switch (ZH / EN / JA), default Chinese
- Multiple themes (Dark / Light / CLI / Christmas)
- Launch at login option
- Graceful tunnel shutdown on app quit

## Build

```bash
swift build
./build.sh
open build/cave.app
```
